# 更新记录

## 2026-09-15 · v1.0.0 系统监控完善

### 系统状态监控

- 入口明确为“左侧菜单 → 系统设置 → 页面顶部第一行右侧”的单一状态按钮。
- 按钮绿色显示“已开启”、灰色显示“已关闭”，点击同一个按钮即可开关系统状态监控。
- 开启后只在第二屏最上层显示半透明圆角性能窗口，可拖动、轻点展开或翻页。
- 关闭后立即移除悬浮窗并停止采样线程、定时任务和前台服务，不产生持续 CPU 开销。
- 悬浮窗宽度收敛为 500 物理像素，避免贴边或遮挡过多副屏内容。
- 本次入口调整不修改“行为记录”的开关、确认流程、服务或数据。

### CPU 与进程统计

- 整机 CPU 改为 `/proc/stat` 严格三秒差分，按 8 核合计的 0–800% 口径显示。
- 普通 App 按 UID 聚合，系统 UID/原生服务按 PID 统计；加入平台签名、无桌面入口的系统采样组件。
- “系统内核 / 其他”承接中断、驱动、内核线程及无法命名的系统消耗，使顶部总量与详情列表闭环。
- 进程名不再添加符号；名称、CPU、MEM 改为三个独立布局列，数值固定宽度并右对齐。
- 当前双屏前台 App 和当前输入法即使本周期为 0%，仍保留在列表中。
- 每个核心显示实时 `scaling_cur_freq`，不再用 1.80GHz 最大频率冒充当前频率；实机确认 0.80/1.20/1.60/1.80GHz 动态调频。

### 活动监控与生命周期

- 清理后台页面使用绑定式 CPU 采样服务，仅在该页面可见时运行；离开页面或退出 App 立即解绑并停止。
- 进程详情补充实时 CPU、PSS、线程、调度、I/O、网络、安装信息及双屏前台保护状态。
- 保留行为记录和文件分发各自独立的用户开关，不被 CPU 页面生命周期误关。

### 验证

- `testDebugUnitTest`、`lintDebug`、`assembleRelease` 全部通过。
- 已在 KEMI Vibe Pads S1（Android 12，HiV730）`192.168.3.63:5555` 覆盖安装验证。
- 同一 App 采样窗口内顶部 CPU 与详情合计差值为 0.00%；与 Android `top` 同时采样时，主要进程绝对差约 0.18–1.72 个百分点。
- 正式主 APK 与系统采样组件位于 `bin/`，SHA-256 记录在 `bin/KEMI-Pads-v1.0.0.sha256`。
